# Cody Hunter Portfolio

React and Tailwind portfolio built with Vite.

## Development

```bash
npm run dev
```

Open the local URL Vite prints, usually `http://localhost:5173/`.

If Vite says port `5173` is already in use, close the older dev server first.

## Build

```bash
npm run build
```
